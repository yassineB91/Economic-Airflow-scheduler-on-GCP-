# DAG-only delivery pipeline. Create a Cloud Build trigger that runs this file
# only when files under dags/ or tests/dags/ change.


substitutions:
  _PROJECT_ID: "dev-env-368414"
  _DAG_VERSION: "manual"
  _VM_NAME: "airflow-vm"
  _VM_ZONE: "europe-west1-b"
  _DAG_ARTIFACT_BUCKET: "dev-env-368414-airflow-dag-artifacts"

steps:

  - id: "publish-dag-artifact"
    name: "gcr.io/google.com/cloudsdktool/cloud-sdk:latest"
    entrypoint: "bash"
    args:
      - "-c"
      - |
        set -Eeuo pipefail

        ARTIFACT_URI="gs://${_DAG_ARTIFACT_BUCKET}/releases/${_DAG_VERSION}/"
        gcloud storage rsync --recursive /workspace/dags "$${ARTIFACT_URI}"
        printf '%s\n' "${_DAG_VERSION}" |
          gcloud storage cp - "gs://${_DAG_ARTIFACT_BUCKET}/releases/${_DAG_VERSION}/REVISION"

        echo "Published DAG artifact: $${ARTIFACT_URI}"

  - id: "deploy-dags-to-vm"
    name: "gcr.io/google.com/cloudsdktool/cloud-sdk:latest"
    waitFor:
      - "publish-dag-artifact"
    entrypoint: "bash"
    args:
      - "-c"
      - |
        set -Eeuo pipefail

        gcloud compute ssh "${_VM_NAME}" \
          --project="${_PROJECT_ID}" \
          --zone="${_VM_ZONE}" \
          --quiet \
          --command='
            set -Eeuo pipefail

            mkdir -p "$$HOME/deploy/dags"
            gcloud storage rsync --recursive \
              --delete-unmatched-destination-objects \
              "gs://${_DAG_ARTIFACT_BUCKET}/releases/${_DAG_VERSION}/" \
              "$$HOME/deploy/dags"

            echo "DAG revision ${_DAG_VERSION} deployed to $$HOME/deploy/dags"
          '

timeout: "900s"

options:
  logging: "CLOUD_LOGGING_ONLY"

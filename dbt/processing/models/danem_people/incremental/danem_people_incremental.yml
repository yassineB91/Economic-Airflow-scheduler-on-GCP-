version: 2
###### Sources ######
sources:
  - name: danem_people
    database: "{{ var('source_project', target.project) }}"
    schema: danem_people
    tables:
      - name: list_links_ext
        columns:
          - name: link
            data_tests:
              - not_null
          - name: insert_date
            data_tests:
              - not_null

###### Models ######

models:
  - name: danem_people_link_list_merge
    description: "merge job links"
    columns:
      - name: insert_date
        data_tests:
          - not_null
    config:
      schema: danem_people

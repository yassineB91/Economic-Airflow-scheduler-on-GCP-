version: 2
###### Sources ######
sources:
  - name: danem_people
    database: "{{ var('source_project', target.project) }}"
    schema: danem_people
    tables:
      - name: danem_cleaning_aggregation


###### Models ######

models:
  - name: danem_modeling_aggregation
    description: "modeling of all jobs"
    columns:
      - name: insert_date
        data_tests:
          - not_null
    config:
      schema: danem_people
